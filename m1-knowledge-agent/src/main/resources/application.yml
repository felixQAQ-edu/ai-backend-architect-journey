server:
  port: 8080

spring:
  application:
    name: knowledge-agent

  # ?????????????????????????????????????????????
  # ?????? ?? ? ADR-001 ????
  # ?? 50 ????? thread-per-request ?????
  # ???????????? >200 ? SSE ??? >5000 ?????
  # threads:
  #   virtual:
  #     enabled: true
  # ?????????????????????????????????????????????

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    com.felix.knowledgeagent: DEBUG