# AI 后端架构师 · 一年学习计划总览

> 本文档是个人学习项目的"中央档案"。每次开新对话时，把这份文档作为上下文上传或粘贴给 Claude，它就能立即理解你的全局规划、当前进度和已做的决策。

---

## 一、项目元信息

| 字段 | 内容 |
|------|------|
| 项目启动日期 | 2026-04-30 |
| 预计完成日期 | 2027-04-30 |
| 当前阶段 | 第一阶段 · 管道优先 |
| 当前月份 | M1 · Week 1 |
| 整体目标 | 从应用层 → 底层推理 → RAG 架构 → 高可用 SaaS，完整跨越四层技术栈 |
| 终极交付物 | 一个上线运营、有付费用户、有可量化业务数据的 AI SaaS 系统 |

### 个人背景与起点

_在这里简要写一下你的现有技术栈、薄弱环节、可投入时间（每周小时数），方便 Claude 在不同窗口里给出更贴合的建议。_

- 主要技术栈：
- 已有项目经验：
- 每周可投入学习时间：
- 薄弱环节：

---

## 二、完整路线图

### 第一阶段 · 管道优先，引擎可换 [M1–M2]

**核心理念**：先跑通架构，再替换业务引擎，避免业务复杂度与技术复杂度叠加。

**设计哲学**：前 8 个月（M1–M8）**刻意不锁定业务方向**，是 feature 不是 bug。M1–M2 验证架构通用性，M3–M5 打造赛道无关的通用能力（本地部署、量化、并发），M6–M7 把 RAG 链路做到能用为止。**真正的方向收敛点在 M8 用户验证阶段**——届时手里会有三张牌：技术能力上限、部署成本真实数据、与潜在用户接触后的反馈。用那时的信息做决策，比现在拍脑袋准得多。现在出现的方向直觉一律录入第七节「方向假设池」备查，不做承诺。

**M1 · Spring Boot × LLM 全链路**
- 流式输出（SSE / WebFlux）、会话记忆（ConversationBufferMemory）
- Token 计费中间件，请求/响应日志落库
- Tool Calling 框架：定义 Function Schema，Agent 调用模拟文档检索 API
- 项目交付：团队知识库问答 Agent（Markdown 文档入库）

**M2 · 引擎替换验证架构通用性**
- 把文档检索 API 换成 Yahoo Finance / Alpha Vantage API
- Agent 自动抓取宏观数据，生成简版策略摘要
- 错误处理、重试机制、多轮对话状态管理

**阶段里程碑**：同一套管道代码支撑两个完全不同领域的 Agent，证明架构解耦成功。

---

### 第二阶段 · 底层部署与推理优化 [M3–M5]

**核心理念**：脱离外部 API，真正的架构师壁垒在这里。

**M3 · 本地模型部署**
- Ollama 跑通 Qwen2.5-7B，对比 API 版本的延迟与质量
- vLLM 搭建，理解 PagedAttention 的 KV Cache 内存分配原理
- Java 后端通过 OpenAI-compatible API 接入本地模型

**M4 · 量化技术实验**
- INT8 / INT4 量化对比：模型大小、推理速度、回答质量三维测评
- GGUF 格式与 llama.cpp 在 CPU 上的执行逻辑
- 不同硬件配置（有无 GPU）下的 tokens/s 数据记录

**M5 · 高并发调度**
- Java 线程池 + 请求队列，模拟 50 并发推理压测
- 瓶颈分析：GPU 显存 vs CPU 调度 vs JVM 线程切换
- Batch Inference：合并小请求，观察吞吐量变化

**阶段里程碑**：能解释为什么 13B 模型在 INT4 量化后能跑在 8GB 显存上，精度损失在哪里。

---

### 第三阶段 · Advanced RAG 核心链路 [M6–M8]

**核心理念**：把 AI 做成领域专家，而不是聪明的复读机。

**M6 · 文档解析与语义分块**
- PDF / 代码库 / LaTeX 文档的结构化解析（PyMuPDF、Unstructured）
- Semantic Chunking：基于句子嵌入相似度动态切分
- 父子分块策略（Parent-Child Chunking）

**M7 · 混合检索 Hybrid Search**
- 向量检索（text-embedding-3 / BGE）+ BM25 关键词检索并行
- RRF（Reciprocal Rank Fusion）融合两路排序结果
- 向量数据库选型实验：Milvus vs Qdrant

**M8 · Reranking + 评估 + 用户验证**
- BGE-Reranker-v2 二次打分，对比 MRR@5 提升幅度
- 评估集（50 条真实问答对），跑 RAGAs 量化幻觉率
- **关键节点**：找 5–10 个目标用户真实使用，收集反馈
- **方向决策节点**：拿第七节「方向假设池」所有候选项做并列对比，结合真实用户反馈选定 M9–M12 的最终业务赛道

**阶段里程碑**：RAG 准确率在评估集上达到 80%+，且有真实用户证明"这个东西有用"。

**选修项**：GraphRAG（若 M6–M7 顺利且有 Neo4j 经验，M8 末可探索）

---

### 第四阶段 · 高可用 SaaS 与商业变现 [M9–M12]

**核心理念**：把九个月积累打包成真正能上线赚钱的系统。

**M9–M10 · 分布式架构升级**
- Redis 分布式锁管理并发 Agent 任务
- Kafka 异步队列：长耗时 AI 任务从同步改异步
- 多租户隔离：数据库 Schema 级 + 向量库 namespace 分区
- 监控接入：Prometheus + Grafana

**M11 · 商业闭环**
- Stripe 支付网关 + Webhook 处理订阅状态
- Token 用量计费：按月订阅 + 超量计费双模式
- 用户仪表板：用量、账单、套餐升级

**M12 · Tech Lead 统筹上线**
- 灰度发布、线上压测、容量规划
- 运营数据看板：DAU、留存率、MRR
- 完整技术架构文档（简历核心材料）

**最终里程碑**：系统上线，有付费用户，有可量化的业务数据 — 不是 demo，是产品。

---

## 三、进度追踪表

> 每周末花 10 分钟更新这张表。状态符号：⬜ 未开始 / 🟨 进行中 / ✅ 已完成 / ⚠️ 阻塞

| 月份 | 阶段 | 核心任务 | 状态 | 实际完成日期 | 主要产出 / 链接 | 备注 |
|------|------|---------|------|------------|---------------|------|
| M1 | 一 | Spring Boot × LLM 全链路 + 团队知识库 Agent | 🟨 | | ADR-001 + ADR-002 已采纳；m1-knowledge-agent/ 脚手架就绪，mock SSE 端点跑通 | Week 1 启动 |
| M2 | 一 | 引擎替换为金融 API | ⬜ | | | |
| M3 | 二 | Ollama + vLLM 本地部署 | ⬜ | | | |
| M4 | 二 | INT8 / INT4 量化对比实验 | ⬜ | | | |
| M5 | 二 | 高并发推理压测 | ⬜ | | | |
| M6 | 三 | Semantic Chunking 与文档解析 | ⬜ | | | |
| M7 | 三 | Hybrid Search 混合检索 | ⬜ | | | |
| M8 | 三 | Reranking + RAGAs 评估 + 用户验证 | ⬜ | | | **方向决策节点**：对比第七节「方向假设池」所有候选项 |
| M9 | 四 | Redis 分布式锁 + Kafka 队列 | ⬜ | | | |
| M10 | 四 | 多租户隔离 + 监控体系 | ⬜ | | | |
| M11 | 四 | Stripe 支付 + 订阅扣费 | ⬜ | | | |
| M12 | 四 | 灰度上线 + 架构文档 | ⬜ | | | |

### 周度学习日志（最近三周，循环覆盖）

**Week 1（2026-04-30）**
- 完成：
  - 总览文档落地，建立项目"中央档案"
  - ADR-001：流式输出技术选型完成（决策：SSE + Spring MVC + SseEmitter）
  - 完成本地开发环境搭建（Homebrew、Git、JDK 21、GitHub SSH），SETUP.md 归档
  - 「方向假设池」首批 5 条候选录入（金融、健身、通用自学、专业考试备考、大学生学习助手）
  - **m1-knowledge-agent/ 脚手架搭建完成**（Spring Boot 3.5.14 + JDK 21）
  - **首个 SSE 端点 `/api/chat/stream` mock 流式输出验证通过**
  - **ADR-002 完成**：Spring Boot 3.5 vs 4.0 版本选型（决策：3.5.14，关键依据是 LangChain4j 尚未兼容 Boot 4）
  - **文档结构整顿**：ADR 内容从 ROADMAP 第四节迁移至 docs/adr/ 独立文件，ROADMAP 第四节改为索引表
- 卡点：暂无
- 下周计划：
  - **ADR-003**：LLM 调用框架选型（LangChain4j vs Spring AI vs 直连 SDK）
  - 接入真实 LLM，把 mock SSE 替换为真流式回复
  - 设计 Token 计费拦截器骨架（基于 Spring MVC Interceptor）

---

## 四、技术决策记录（ADR）

> 每次做出重要技术选型时，新增一条 ADR 文件到 [docs/adr/](./adr/) 目录。这是简历和复盘时最值钱的资产。
> 命名规则：`ADR-NNN-决策主题简短描述.md`

### ADR 索引

| 编号 | 标题 | 状态 | 日期 |
|------|------|------|------|
| [ADR-001](./adr/ADR-001-sse-vs-webflux.md) | Spring Boot LLM 流式输出技术选型（SSE vs WebFlux） | 已采纳 | 2026-04-30 |
| [ADR-002](./adr/ADR-002-spring-boot-version.md) | Spring Boot 版本选型（3.5 vs 4.0） | 已采纳 | 2026-04-30 |
| ADR-003 | _候选议题：LLM 调用框架选型（LangChain4j vs Spring AI vs 直连 SDK）_ | 提议中 | — |

### ADR 模板（新增 ADR 时复制此结构）

新建文件 `docs/adr/ADR-NNN-决策主题.md`，内容结构：

- **标题**：`# ADR-NNN · _决策标题_`
- **元信息**：日期、状态（提议中 / 已采纳 / 已废弃 / 被 ADR-YYY 取代）
- **背景**：当时面临什么问题，约束条件是什么
- **候选方案**：每个方案的优缺点（至少两个候选，否则不构成"决策"）
- **最终决策**：选了哪个，关键理由
- **已知代价**：这个决策放弃了什么，未来需要重新审视的触发条件
- **实际效果**（事后补充）：实施 N 周后的真实反馈

具体写法可参考已有的 [ADR-001](./adr/ADR-001-sse-vs-webflux.md) 和 [ADR-002](./adr/ADR-002-spring-boot-version.md)。

---

## 五、关键资源索引

### 必读资料

- [ ] vLLM 官方文档：PagedAttention 论文与实现
- [ ] LangChain4j 文档（Java 生态的 LLM 框架）
- [ ] BGE-Reranker / BGE-M3 模型卡片
- [ ] RAGAs 评估框架文档
- [ ] Stripe API 官方教程

### 参考项目（可拆解学习的开源项目）

- [ ] 项目 1：_例如 Dify、FastGPT 等开源 RAG 平台_
- [ ] 项目 2：
- [ ] 项目 3：

### 课程 / 视频

- [ ]

### 用户验证渠道备忘（M8 阶段使用）

> 提前记录的低成本传播路径，避免 M8 临阵两眼一抹黑。
> **核心原则**：渠道是产品的乘数，不是加数。产品本身解决真痛点时渠道才有意义。

- **寄生在已有社区**（首选）：去目标用户聚集的鱼塘真实回答问题，顺手提产品。国内：即刻 / 小红书 / 知乎 / V2EX / 少数派；海外：Reddit 对应 subreddit / Hacker News（Show HN）/ Product Hunt / Twitter
- **写"过程"而不是"产品"**：把 ADR 这种深度技术决策做成连载，发掘金 / InfoQ / Medium。简历和影响力双倍收益，产品没做成写作本身也值钱
- **找 10 个不是 1000 个**：M8 阶段目标只是 5–10 个真实用户，朋友圈、群、前同事直接问足够，不用造流量。真正难的是后续找愿意付钱的人
- **冷启动套路**：免费给一个有名的开源项目 / 机构做定制部署，借光被看见（"build in public + lighthouse user"）

---

## 六、Claude 协作使用说明

> 这一节是给 Claude 看的协作指引。新对话开始时，Claude 读到这里能立刻进入状态。

### 文档同步约定

本项目的中央档案分布在仓库的 `docs/` 目录下，权威版本在 GitHub。
**Project Knowledge 是只读快照**，需要手动同步。

每次开新对话前，确保 Project Knowledge 里有以下文件的最新版本：

- `docs/ROADMAP.md`（必传，每次更新都要重传）
- `docs/SETUP.md`（环境基线，变化时重传）
- `docs/adr/ADR-*.md`（**所有 ADR 文件都要传**，新增或修改后重传对应文件）

> 💡 ADR 文件是"权威决策快照"。如果不传，Claude 在新对话里只能从 ROADMAP 第四节的索引表看到 ADR 标题和状态，**无法读到决策细节**。这会导致 Claude 在引用历史决策时只能复述标题、无法还原推理逻辑。

每次更新 ADR / ROADMAP 后的标准动作：

```
1. 编辑文件 → 2. git commit + push → 3. 把改动的文件重新上传到 Project Knowledge
```

如果某次新对话 Claude 发现引用的 ADR 内容与最新提交不一致，**Claude 应主动提示用户重新上传**，而不是基于过期版本继续推理。

### 协作行为约定

**当我开新对话时，请：**

1. 默认我已经在某个月份的某个具体任务上，先看"进度追踪表"了解当前状态，再回答问题
2. 如果我的提问跨越多个阶段，引用本文档对应章节，避免重复解释整体规划
3. 涉及技术选型时，**主动建议新增一条 ADR**，并提供模板填充内容
4. 不要重复解释路线图本身，除非我明确要求修订
5. 每次给出长篇技术建议后，**主动询问是否需要更新本文档的对应章节**
6. 当对话进入开放式探讨（方向选择、商业判断、产品形态等模糊议题）时，**每段讨论结束前主动询问是否需要做结构化总结**，并提示哪些内容值得入档（ADR / 方向假设池 / 周度日志 / 资源索引）。这是本项目验证有效的工作循环：用户抛模糊直觉 → Claude 做结构化展开 + 风险扫描 + 横向对比 → 用户结合真实判断收敛 → 形成可入档结论
7. 引用 ADR 时直接说"ADR-002 第二条已知代价"这样的精确锚点，**不要泛泛说"之前的某个决策"**

**当前对话焦点**：_每次开新窗口时，在这里写一句话说明本次想聊什么_

例如：
- "本次焦点：M3 vLLM 部署，遇到显存 OOM 问题，需要排查"
- "本次焦点：为 M7 做 Milvus vs Qdrant 选型决策"
- "本次焦点：往方向假设池新增一条候选并做四问评估"

---

## 七、方向假设池

> 不锁定方向，但**结构化记录**每一次方向直觉。M8 用户验证阶段拿这张表所有候选项做并列对比，结合真实用户反馈做最终决策。开新对话冒出新方向想法时，按下方结构追加一条即可。

### 判断方向的四问框架

每条候选都用这四个问题快速过一遍，全部答得漂亮的方向才是好方向：

1. **我的技术栈（RAG）能在这里发挥到极致吗？** — 离 RAG 越远的场景越不适合
2. **ChatGPT 在这里答不准吗？** — 如果通用大模型已经够用，产品就没意义
3. **这里有没有合规雷区？** — 金融、医疗、法律是三大雷区
4. **用户在哪里？我够得着吗？** — 用户聚集且渠道清晰的方向优先

### 已录入候选（截至 2026-04-30）

| 方向 | 技术匹配 | ChatGPT 替代风险 | 合规风险 | 渠道难度 | 综合判断 | 录入日期 |
|------|---------|------------------|---------|---------|---------|---------|
| 金融分析 / 投研助手 | 强 | 低（实时性 + 时效性） | **高**（投资咨询牌照） | 中 | 高回报高风险，需在产品定位上严格绕开"建议"话术 | 2026-04-30 |
| 健身 / 卡路里计算 | 弱（核心是图像识别 + 数据库查询，非 RAG） | 高 | 中（健康建议） | 中 | 不推荐 | 2026-04-30 |
| 通用自学助手 | 中 | **极高**（差异化为零） | 低 | 高 | 不推荐 | 2026-04-30 |
| 专业考试备考（CFA/CPA/法考/考研专业课） | **极强** | 低（专业 + 本土 + 时效） | 低 | 中 | 强烈推荐评估 | 2026-04-30 |
| 大学生课程学习助手（PPT 解析 + 跨章节问答） | **极强** | 低（PPT 解析 + 跨文档检索是杀手锏） | 低 | 低（用户聚集，渠道清晰） | 强烈推荐评估 | 2026-04-30 |

### 候选详情备忘

**金融分析 / 投研助手**
- 趋势支撑：中国年轻一代资产配置觉醒（房产逻辑衰退、存款利率下行、对工资养老的不信任），可能是持续 10 年的结构性机会
- 优势：信息有时效性 + 有结构（财报/研报/公告标准格式）+ 用户付费意愿强（影响投资决策的信息付费意愿是普通知识的 10 倍量级，参考 Bloomberg 终端 24000 美金/年的底层逻辑）
- **隐藏陷阱：合规深水区**。在中国向用户提供"投资建议"需证券投资咨询牌照，回答里禁止出现买卖建议。可绕开但需在产品定位上严格设计为"信息检索工具"而非"决策助手"。M8 用户验证时必须明确测试"产品定位能不能既有用又不踩线"
- 其他风险：高质量数据 API 成本高（Wind / 同花顺）、Yahoo Finance 在 A 股覆盖有限；散户对 AI 的"帮我赚钱"预期会反噬产品口碑；东方财富 / 雪球等平台已在加 AI 功能

**专业考试备考**
- 完美匹配 RAG 的所有特征：内容有边界（指定教材 + 历年真题 + 考纲）、用户焦虑且付费意愿强（一次考试 = 半年到一年的人生节点）、ChatGPT 答不准（专业术语 + 本土考点 + 政策更新）、市场分散无通吃者（每个细分考试都是独立机会）
- 可选细分：CFA / CPA / 法考 / 公务员 / 考研专业课
- 风险：内容版权（教材授权问题）、考纲变化的内容更新成本

**大学生课程学习助手**
- 场景定位：不只是"考前划重点"，是**整个学期的学习陪伴工具**——几十 G 讲义跨文档检索、跨章节知识点串联、精准引用页码
- 用户画像：每年 1000 多万在校生，用户高度集中、渠道清晰（小红书 / B 站 / 校园社群），自己是潜在用户的近邻人群，需求验证不用跑很远
- 技术栈匹配度极高：PDF/PPT 解析（M6 直接对口）+ 跨文档检索（M7）+ 精准引用（M8 最容易出"哇"时刻）
- 头部空白：Notion AI / 夸克都在做相关功能，但都是通用产品的子功能，不是为"中国大学生学习场景"专门设计的垂直产品
- **合规风险最低**，相对金融、医疗、法律方向有明显优势

### 待评估（开新对话时若有新想法，按上方结构追加）

_暂无_

---

## 八、版本历史

| 版本 | 日期 | 修订内容 |
|------|------|---------|
| v1.0 | 2026-04-30 | 初始路线图与模板创建 |
| v1.1 | 2026-04-30 | M1 Week 1 启动：填入项目元信息、ADR-001 流式输出选型完成、周度日志首条记录 |
| v1.2 | 2026-04-30 | 嵌入 ADR-001 完整内容（SSE + Spring MVC + SseEmitter 决策详情、已知代价与重审触发条件）；ADR-002 候选议题预占位 |
| v1.3 | 2026-04-30 | 新增第七节「方向假设池」（四问框架 + 5 条候选首批录入）；第一阶段补充"前 8 个月不锁方向"设计哲学；M8 标记为方向决策节点；关键资源索引下补充用户验证渠道备忘；Claude 协作约定新增第 6 条（开放式探讨需主动总结入档） |
| v1.4 | 2026-04-30 | M1 W1 收官：m1-knowledge-agent/ 脚手架就绪 + mock SSE 端点跑通；新增 ADR-002（Boot 3.5 vs 4.0 版本选型）；ROADMAP 第四节由"嵌入 ADR 全文"改为"索引表"，ADR 全文迁出到 docs/adr/ 独立文件；第六节新增「文档同步约定」明确 Project Knowledge 上传规范 |
