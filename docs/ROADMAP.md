# AI 后端架构师 · 一年学习计划总览

> 本文档是个人学习项目的"中央档案"。每次开新对话时，把这份文档作为上下文上传或粘贴给 Claude，它就能立即理解你的全局规划、当前进度和已做的决策。

---

## 一、项目元信息

| 字段 | 内容 |
|------|------|
| 项目启动日期 | 2026-04-30 |
| 预计完成日期 | 2027-04-30 |
| 当前阶段 | 第一阶段 · 管道优先 |
| 当前月份 | M1 · Week 1 |
| 整体目标 | 从应用层 → 底层推理 → RAG 架构 → 高可用 SaaS，完整跨越四层技术栈 |
| 终极交付物 | 一个上线运营、有付费用户、有可量化业务数据的 AI SaaS 系统 |

### 个人背景与起点

_在这里简要写一下你的现有技术栈、薄弱环节、可投入时间（每周小时数），方便 Claude 在不同窗口里给出更贴合的建议。_

- 主要技术栈：
- 已有项目经验：
- 每周可投入学习时间：
- 薄弱环节：

---

## 二、完整路线图

### 第一阶段 · 管道优先，引擎可换 [M1–M2]

**核心理念**：先跑通架构，再替换业务引擎，避免业务复杂度与技术复杂度叠加。

**M1 · Spring Boot × LLM 全链路**
- 流式输出（SSE / WebFlux）、会话记忆（ConversationBufferMemory）
- Token 计费中间件，请求/响应日志落库
- Tool Calling 框架：定义 Function Schema，Agent 调用模拟文档检索 API
- 项目交付：团队知识库问答 Agent（Markdown 文档入库）

**M2 · 引擎替换验证架构通用性**
- 把文档检索 API 换成 Yahoo Finance / Alpha Vantage API
- Agent 自动抓取宏观数据，生成简版策略摘要
- 错误处理、重试机制、多轮对话状态管理

**阶段里程碑**：同一套管道代码支撑两个完全不同领域的 Agent，证明架构解耦成功。

---

### 第二阶段 · 底层部署与推理优化 [M3–M5]

**核心理念**：脱离外部 API，真正的架构师壁垒在这里。

**M3 · 本地模型部署**
- Ollama 跑通 Qwen2.5-7B，对比 API 版本的延迟与质量
- vLLM 搭建，理解 PagedAttention 的 KV Cache 内存分配原理
- Java 后端通过 OpenAI-compatible API 接入本地模型

**M4 · 量化技术实验**
- INT8 / INT4 量化对比：模型大小、推理速度、回答质量三维测评
- GGUF 格式与 llama.cpp 在 CPU 上的执行逻辑
- 不同硬件配置（有无 GPU）下的 tokens/s 数据记录

**M5 · 高并发调度**
- Java 线程池 + 请求队列，模拟 50 并发推理压测
- 瓶颈分析：GPU 显存 vs CPU 调度 vs JVM 线程切换
- Batch Inference：合并小请求，观察吞吐量变化

**阶段里程碑**：能解释为什么 13B 模型在 INT4 量化后能跑在 8GB 显存上，精度损失在哪里。

---

### 第三阶段 · Advanced RAG 核心链路 [M6–M8]

**核心理念**：把 AI 做成领域专家，而不是聪明的复读机。

**M6 · 文档解析与语义分块**
- PDF / 代码库 / LaTeX 文档的结构化解析（PyMuPDF、Unstructured）
- Semantic Chunking：基于句子嵌入相似度动态切分
- 父子分块策略（Parent-Child Chunking）

**M7 · 混合检索 Hybrid Search**
- 向量检索（text-embedding-3 / BGE）+ BM25 关键词检索并行
- RRF（Reciprocal Rank Fusion）融合两路排序结果
- 向量数据库选型实验：Milvus vs Qdrant

**M8 · Reranking + 评估 + 用户验证**
- BGE-Reranker-v2 二次打分，对比 MRR@5 提升幅度
- 评估集（50 条真实问答对），跑 RAGAs 量化幻觉率
- **关键节点**：找 5–10 个目标用户真实使用，收集反馈

**阶段里程碑**：RAG 准确率在评估集上达到 80%+，且有真实用户证明"这个东西有用"。

**选修项**：GraphRAG（若 M6–M7 顺利且有 Neo4j 经验，M8 末可探索）

---

### 第四阶段 · 高可用 SaaS 与商业变现 [M9–M12]

**核心理念**：把九个月积累打包成真正能上线赚钱的系统。

**M9–M10 · 分布式架构升级**
- Redis 分布式锁管理并发 Agent 任务
- Kafka 异步队列：长耗时 AI 任务从同步改异步
- 多租户隔离：数据库 Schema 级 + 向量库 namespace 分区
- 监控接入：Prometheus + Grafana

**M11 · 商业闭环**
- Stripe 支付网关 + Webhook 处理订阅状态
- Token 用量计费：按月订阅 + 超量计费双模式
- 用户仪表板：用量、账单、套餐升级

**M12 · Tech Lead 统筹上线**
- 灰度发布、线上压测、容量规划
- 运营数据看板：DAU、留存率、MRR
- 完整技术架构文档（简历核心材料）

**最终里程碑**：系统上线，有付费用户，有可量化的业务数据 — 不是 demo，是产品。

---

## 三、进度追踪表

> 每周末花 10 分钟更新这张表。状态符号：⬜ 未开始 / 🟨 进行中 / ✅ 已完成 / ⚠️ 阻塞

| 月份 | 阶段 | 核心任务 | 状态 | 实际完成日期 | 主要产出 / 链接 | 备注 |
|------|------|---------|------|------------|---------------|------|
| M1 | 一 | Spring Boot × LLM 全链路 + 团队知识库 Agent | 🟨 | | ADR-001 流式输出选型已确定（SSE + Spring MVC） | Week 1 启动 |
| M2 | 一 | 引擎替换为金融 API | ⬜ | | | |
| M3 | 二 | Ollama + vLLM 本地部署 | ⬜ | | | |
| M4 | 二 | INT8 / INT4 量化对比实验 | ⬜ | | | |
| M5 | 二 | 高并发推理压测 | ⬜ | | | |
| M6 | 三 | Semantic Chunking 与文档解析 | ⬜ | | | |
| M7 | 三 | Hybrid Search 混合检索 | ⬜ | | | |
| M8 | 三 | Reranking + RAGAs 评估 + 用户验证 | ⬜ | | | |
| M9 | 四 | Redis 分布式锁 + Kafka 队列 | ⬜ | | | |
| M10 | 四 | 多租户隔离 + 监控体系 | ⬜ | | | |
| M11 | 四 | Stripe 支付 + 订阅扣费 | ⬜ | | | |
| M12 | 四 | 灰度上线 + 架构文档 | ⬜ | | | |

### 周度学习日志（最近三周，循环覆盖）

**Week 1（2026-04-30）**
- 完成：
  - 总览文档落地，建立项目"中央档案"
  - ADR-001：流式输出技术选型完成（决策：SSE + Spring MVC + SseEmitter）
  - 完成本地开发环境搭建（Homebrew、Git、JDK 21、GitHub SSH），SETUP.md 归档
- 卡点：暂无
- 下周计划：
  - 搭建 Spring Boot 3.x 项目脚手架（JDK 21，启用虚拟线程作为后续升级路径准备）
  - 选定 LLM 调用框架（潜在 ADR-002）
  - 实现首个 SSE 端点的 mock 流式输出

---

## 四、技术决策记录（ADR）

> 每次做出重要技术选型时，新增一条记录。这是简历和复盘时最值钱的资产。
> 命名规则：ADR-NNN-决策主题简短描述

### ADR 模板

```markdown
### ADR-XXX · _决策标题_

- 日期：YYYY-MM-DD
- 状态：[提议中 / 已采纳 / 已废弃 / 被 ADR-YYY 取代]
- 背景：_当时面临什么问题，约束条件是什么_
- 候选方案：
  1. 方案 A — 优点 / 缺点
  2. 方案 B — 优点 / 缺点
  3. 方案 C — 优点 / 缺点
- 最终决策：_选了哪个，关键理由_
- 已知代价：_这个决策放弃了什么，未来可能需要重新审视的条件_
- 实际效果（事后补充）：_实施 N 周后的真实反馈_
```

### ADR-001 · Spring Boot LLM 流式输出技术选型（SSE vs WebFlux）

- 日期：2026-04-30
- 状态：已采纳
- 背景：
  M1 阶段需要在 Spring Boot 后端打通"用户提问 → LLM 流式生成 → 前端逐 token 渲染"的全链路。
  约束条件：
  1. 单机预计承载并发量 ≤ 50（M5 压测目标也是 50 并发，量级一致）
  2. 当前是学习计划第一周，需要把精力集中在"管道打通 + Tool Calling + 计费中间件"等业务主线，
     调试体验越接近传统 Spring MVC 越好
  3. 数据流是单向的（服务端 → 客户端逐 token 推送），不需要双向通信
  4. 后端会调用阻塞式的 LLM SDK / OpenAI-compatible HTTP 客户端，并不是端到端响应式栈

- 候选方案：

  1. **SSE + Spring MVC（SseEmitter）**
     - 优点：
       - 同步阻塞编程模型，断点调试、stack trace、@Transactional 行为都是熟悉的 MVC 语义
       - SseEmitter API 简单直接：new + send() + complete()，一个下午能跑通
       - 与现有 Servlet 生态（Filter / Interceptor / Spring Security）无缝兼容，
         Token 计费中间件、请求日志落库这些 M1 任务可以直接用拦截器实现
       - 浏览器原生支持 EventSource，前端一行代码接入
       - 升级路径平滑：未来若并发压力上来，application.yml 加一行
         `spring.threads.virtual.enabled=true` 即可获得接近响应式的吞吐量，业务代码零改动
     - 缺点：
       - thread-per-request 模型，每个 SSE 连接占一个 Servlet 线程
       - 在 Tomcat 默认 200 max-threads 下，理论并发上限约 200，远超本项目 50 的需求，
         但确实不是为"5 万 SSE 长连接"这种场景设计的

  2. **WebFlux + Flux<ServerSentEvent>（Reactor）**
     - 优点：
       - 事件循环架构，少量线程支撑海量长连接，是 Netty + 响应式栈的"亲儿子"用法
       - Backpressure 原生支持，能从消费端反向控制生产速度
       - 对真正的全栈响应式（R2DBC + Reactive Redis + Reactive Kafka）项目是最优解
     - 缺点：
       - 学习曲线陡：Mono / Flux / Schedulers / Context 传播都不是入门级概念，
         M1 第一周引入会显著拖慢主线进度
       - 调试体验差：stack trace 被 Reactor 操作符切碎，断点常常落不到预期位置
       - 阻塞式 LLM SDK 必须手动包到 `boundedElastic` 调度器，
         否则会阻塞事件循环线程 —— 这一步出错就退化成"假响应式"，性能反而比 MVC 还差
       - JPA / @Transactional 与响应式不兼容，后续若引入关系数据库会有撕裂感
       - 对 50 并发的场景属于明显过度设计

  3. **WebSocket**
     - 优点：双向全双工通信
     - 缺点：本场景只需服务端单向推送，WebSocket 增加握手协议、心跳维护、断线重连等额外复杂度，
       属于明显过度设计。排除。

- 最终决策：**方案 1 — SSE + Spring MVC（SseEmitter）**

  关键理由：
  - **匹配阶段目标**：M1 的核心是"管道优先"，技术选型应服务于"快速跑通全链路"，
    而不是引入新的认知负担。WebFlux 在 50 并发场景下的性能优势在本项目里观察不到，
    但其复杂度成本是确实要支付的。
  - **匹配并发量级**：50 并发对 thread-per-request 模型完全是舒适区，
    Tomcat 默认配置即可，无需任何调优。
  - **调试体验与生态兼容**：SseEmitter 走的是标准 Servlet 异步分支，
    所有 Spring MVC 的拦截器、AOP、@Transactional、MDC 日志都正常工作，
    M1 的 Token 计费中间件、请求日志落库可以直接复用熟悉模式。
  - **保留升级路径**：如果未来真的需要支撑万级长连接（极不可能在本项目发生），
    第一选择是开启 Java 21 虚拟线程而非重写为 WebFlux ——
    业务代码无需改动，单行配置即可获得接近响应式的吞吐量。

- 已知代价：
  1. 放弃了 Reactor 的 backpressure 能力。LLM 流式场景里这个能力的实际价值有限
     （token 推送速率由模型本身决定，前端处理速度通常不是瓶颈），可接受。
  2. 放弃了响应式栈的"心智一致性"。如果后续 M9–M10 要引入 Reactive Kafka / R2DBC，
     需要重新审视这个决策。届时若决定切换，预计改造点：
     Controller 层重写（约 3 个端点）、调用链改用 Mono/Flux、阻塞 SDK 包到 Schedulers。
  3. 重新审视的触发条件：
     - 实测并发稳定超过 200，或
     - 整个技术栈已演进为全响应式（关系库换 R2DBC、缓存换 Reactive Redis），或
     - SSE 长连接数量级达到 5000+

- 实际效果（事后补充）：
  _M1 结束时回填：实际跑通用了多久？调试体验是否符合预期？是否遇到 SseEmitter 的坑？_

### ADR-002 · _下一个待记录的决策（候选：LLM 调用框架选型 — LangChain4j vs Spring AI vs 直连 SDK）_

---

## 五、关键资源索引

### 必读资料

- [ ] vLLM 官方文档：PagedAttention 论文与实现
- [ ] LangChain4j 文档（Java 生态的 LLM 框架）
- [ ] BGE-Reranker / BGE-M3 模型卡片
- [ ] RAGAs 评估框架文档
- [ ] Stripe API 官方教程

### 参考项目（可拆解学习的开源项目）

- [ ] 项目 1：_例如 Dify、FastGPT 等开源 RAG 平台_
- [ ] 项目 2：
- [ ] 项目 3：

### 课程 / 视频

- [ ]

---

## 六、Claude 协作使用说明

> 这一节是给 Claude 看的协作指引。新对话开始时，Claude 读到这里能立刻进入状态。

**当我开新对话时，请：**

1. 默认我已经在某个月份的某个具体任务上，先看"进度追踪表"了解当前状态，再回答问题
2. 如果我的提问跨越多个阶段，引用本文档对应章节，避免重复解释整体规划
3. 涉及技术选型时，**主动建议新增一条 ADR**，并提供模板填充内容
4. 不要重复解释路线图本身，除非我明确要求修订
5. 每次给出长篇技术建议后，**主动询问是否需要更新本文档的对应章节**

**当前对话焦点**：_每次开新窗口时，在这里写一句话说明本次想聊什么_

例如：
- "本次焦点：M3 vLLM 部署，遇到显存 OOM 问题，需要排查"
- "本次焦点：为 M7 做 Milvus vs Qdrant 选型决策"

---

## 七、版本历史

| 版本 | 日期 | 修订内容 |
|------|------|---------|
| v1.0 | 2026-04-30 | 初始路线图与模板创建 |
| v1.1 | 2026-04-30 | M1 Week 1 启动：填入项目元信息、ADR-001 流式输出选型完成、周度日志首条记录 |
| v1.2 | 2026-04-30 | 嵌入 ADR-001 完整内容（SSE + Spring MVC + SseEmitter 决策详情、已知代价与重审触发条件）；ADR-002 候选议题预占位 |
